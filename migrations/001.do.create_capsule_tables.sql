CREATE TABLE capsules (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    title TEXT NOT NULL,
    note TEXT NOT NULL,
    imageurl TEXT,
    burydate TIMESTAMP NOT NULL DEFAULT now(),
    unlockdate TIMESTAMP NOT NULL DEFAULT now()
)